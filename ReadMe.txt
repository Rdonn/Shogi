To setup the database, use mysql to source 'load.sql'

First, run the 'start_server.bat'
Then, verify the server information in the Server GUI
Next, press the 'Listen' button

Second, edit the 'start_client.bat' to have the server's address as the first argument and the port as the second argument
Next, run the 'start_client.bat'